{
  "type": "module",
  "scripts": {
    "sh:db": "docker compose run --rm db bash",
    "sh:pgadmin": "docker compose run --rm pgadmin bash",
    "sh:backend": "docker compose run --rm backend bash",
    "sh:frontend": "docker compose run --rm frontend bash"
  },
  "dependencies": {}
}
